[project]
name = "term-project-chimera"
version = "0.1.0"
description = "Term project for NLD course"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "scipy>=1.17.0",
]

[tool.setuptools]
packages = ["src"]
